# 光标设置
env = HYPRCURSOR_SIZE,24
env = XCURSOR_SIZE,24

cursor {
    hide_on_key_press = true
    # 光标不活动多少秒后隐藏它
    inactive_timeout = 5
    hide_on_touch = true
    # 测试 vmware 的虚拟机里光标没有显示，设置 no_hardware_cursors 为 true 可以解决
    # FIXME: https://github.com/hyprwm/Hyprland/issues/7577
    # 定期检查新版本是否已经解决
    no_hardware_cursors = true
}
